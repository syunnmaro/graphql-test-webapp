query getTodo {
  todos {
    id
    text
  }
}

mutation CreateTodo {
    createTodo(input: {text: "Create GraphQL Example", done: false, userID: 1}) {
        id
        text
        done
        userID
    }
}
